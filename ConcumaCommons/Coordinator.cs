namespace ConcumaCommons
{
    public static class Coordinator
    {
        private static readonly Map<Type, byte> _statementBytecode = new();
        private static readonly Map<Type, byte> _expressionBytecode = new();

        static Coordinator()
        {
            _statementBytecode.Add()
        }
    }
}